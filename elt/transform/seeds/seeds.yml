version: 2

seeds:
  - name: porciuncula_prefeitura_entities
    description: "Entidades municipais de Porciúncula (prefeitura + fundos)"
    config:
      schema: seeds
    columns:
      - name: portal_slug
        data_type: varchar
      - name: empresa_id
        data_type: varchar
      - name: nome
        data_type: varchar
